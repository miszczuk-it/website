# Base URL of the IoT backend (public, not a secret). Defaults to production in code if unset,
# so this only needs to be set locally to point at a backend running on localhost.
VITE_IOT_API_URL=http://localhost:8080
